import { StyleSheet } from "react-native";
import { buttonBackground,pageBackground,white } from "../../constants/colors";
export default StyleSheet.create({
    modalContainer:{
        height:300
    },
    modalWrapper:{
        backgroundColor:pageBackground,
        flex:1,
        borderRadius:30
    }
   
});